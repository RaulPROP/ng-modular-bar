import { InsidePercentagePipe } from './inside-percentage.pipe';

// TODO: Write tests
describe('InsidePercentagePipe', () => {
	it('create an instance', () => {
		const pipe = new InsidePercentagePipe();
		expect(pipe).toBeTruthy();
	});
});
